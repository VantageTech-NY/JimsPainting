---
import { Image } from "astro:assets";
import { BUSINESS, NAV } from "../consts";
import PhoneIcon from "./PhoneIcon.astro";
import logo from "../assets/logo.jpg";

// Highlight the current top-level section.
const path = Astro.url.pathname;
const isActive = (href: string) =>
  href === "/" ? path === "/" : path.startsWith(href);
---

<a class="skip-link" href="#main">Skip to content</a>

<header class="site-header">
  <div class="header-inner container">
    <a class="brand" href="/">
      <Image class="brand-logo" src={logo} alt="" width={92} height={92} loading="eager" />
      <span>{BUSINESS.name}</span>
    </a>

    <button
      class="nav-burger"
      type="button"
      aria-expanded="false"
      aria-controls="primary-nav"
    >
      Menu
    </button>

    <nav class="primary-nav" id="primary-nav" aria-label="Primary">
      <ul class="nav-list">
        {
          NAV.map((item) => (
            <li>
              <a
                href={item.href}
                aria-current={isActive(item.href) ? "page" : undefined}
              >
                {item.label}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>

    {/* Phone is visible on every page — calls convert best for this audience. */}
    <a class="header-phone" href={BUSINESS.phoneHref}>
      <PhoneIcon /> {BUSINESS.phone}
    </a>
  </div>
</header>
